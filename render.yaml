services:
  - type: web
    name: ambp
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /health
    autoDeploy: true
    region: singapore